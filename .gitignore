tags
test.sh
.luarc.json
nvim

spell/

# NOTE: lazy-lock.json is deliberately tracked (upstream kickstart ignores it).
# Committing it pins exact plugin revisions, so `:Lazy restore` can always put
# the config back to a known-good state after an update goes wrong.
